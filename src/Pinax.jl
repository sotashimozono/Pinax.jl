module Pinax

using ParamIO
using DataVault

# Pinax — 図録 DSL + gallery renderer。
# 設計仕様は notes/(00–10)を参照。実装はこれから:
#   document.jl / resolve.jl / render.jl / cache.jl / theme/gallery/ / backends.jl / diagnostics.jl

end # module Pinax
